doctype html

html
  head
    meta(charset='UTF-8')
    title React chrome-extension (Popup)
    style.
      body { width: 500px; }

  body
    #root
    script(src=env == 'prod' ? '/js/popup.bundle.js' : 'http://localhost:3000/js/popup.bundle.js')
